# Verification workflow

This app has no runtime demo mode or seeded trips. Follow README.md for normal use.

Mock data exists only in automated tests and the explicitly invoked tests/browser_server.py acceptance harness, which uses a temporary database. The normal application never imports it. See VERIFICATION.md for results.
